spring:
  datasource:
    url: jdbc:postgresql://db:5432/shorturl
    username: shorturl
    password: shorturlpwpwpw
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        id:
          new_generator_mappings: true
    open-in-view: false

  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
